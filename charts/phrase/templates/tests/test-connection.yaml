apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phrase.fullname" . }}-test-connection"
  labels:
    {{- include "phrase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "phrase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
